import React from "react";

const CameraIcon = ({ style }) => (
  <i
    className="fa-solid text-primary fa-camera text-dark"
    style={{ ...style, marginLeft: "-1rem" }}
  ></i>
);

export default CameraIcon;
